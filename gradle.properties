# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G

# Default Minecraft target (override with -PmcVersion)
minecraft_version=1.21.7

# Note: Yarn/Fabric API/Loader are now selected in build.gradle
# based on the chosen mcVersion. These properties are left out to
# prevent drift.

# Mod Properties
mod_version=1.0.0
maven_group=ltown.blackmesa
archives_base_name=hev_suit

# Dependencies
# check this on https://modmuss50.me/fabric.html
